# Pages

Route-level screens, one file per page.

**Built so far**
- `Login.jsx` — username + password (slice 1)
- `ClientList.jsx` — dashboard: all clients at a glance, add/edit/delete (slice 2)
- `ClientDetail.jsx` — individual client profile; progress (slice 3), workout
  plan/log/overload+PR (slice 4), diet (slice 5), and check-ins + coach's log
  (slice 6) sections render inside here

**Planned (added as build slices land)**
- Gamification and the coach/personal layer
