# Pages

Route-level screens, one file per page.

**Built so far**
- `Login.jsx` — username + password (slice 1)
- `ClientList.jsx` — dashboard: all clients at a glance, add/edit/delete (slice 2)
- `ClientDetail.jsx` — individual client profile; progress (slice 3) and workout
  plan/log/overload+PR sections (slice 4) render inside here

**Planned (added as build slices land)**
- Diet, check-ins, gamification, and the coach/personal layer
